# Vercel Blob store token used by /api/upload to host car photos uploaded
# from the /admin editor. Create a Blob store in the Vercel dashboard
# (Storage -> Create Database -> Blob) to get this value.
BLOB_READ_WRITE_TOKEN=
